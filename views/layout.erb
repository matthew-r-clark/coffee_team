<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <link rel="stylesheet" href="/styles.css" type="text/css">
    <title>Coffee Team</title>
    <!--Favicon--><link rel="icon" type="image/png" href="https://i.imgur.com/UksUSEq.png">
</head>
<body>
  <header class="main-header">
    <a href="/"><img id="logo" src="https://i.imgur.com/kdWNxYO.png"></a>
    <ul class="nav-menu">
      <li><a href="/about" class="nav-button">About</a></li>
      <li><a href="/charts" class="nav-button">Charts</a></li>
      <li><a href="/community" class="nav-button">Community</a></li>
      <% if logged_in? %>
        <li>
          <a href="/logout" class="signin">Logout</a>
          <a href="/profile/<%= get_username %>" class="signin"><%= get_username %></a>
        </li>
      <% else %>
        <li><a href="/signin" class="signin">Signin</a></li>
      <% end %>
    </ul>
  </header>

  <div id="sub-menu">
    <ul class="nav-menu">
      <%== yield_content :sub_menu %>
    </ul>
  </div>

  <div id="flash-message">
    <% if session[:error] %>
      <div id="error"><%= session.delete(:error) %></div>
    <% elsif session[:success] %>
      <div id="success"><%= session.delete(:success) %></div>
    <% end %>
  </div>

  <%== yield %>
</body>
</html>